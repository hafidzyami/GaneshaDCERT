name: Deploy GaneshaDCERT to VPS (Production)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: PROD

    env:
      PORT: ${{ secrets.PORT }}
      RABBITMQ_URL: ${{ secrets.RABBITMQ_URL }}
      MESSAGE_TTL: ${{ secrets.MESSAGE_TTL }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Deploy to Production Server
        if: github.ref == 'refs/heads/main'
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST_PROD }}
          username: ${{ secrets.VPS_USER_PROD }} # <-- Gunakan secrets untuk PROD
          key: ${{ secrets.VPS_SSH_KEY_PROD }} # <-- Gunakan secrets untuk PROD
          script: |
            # Pindah ke direktori aplikasi
            cd /var/www/ganeshadcert-prod

            # 1. Hentikan semua container yang sedang berjalan
            # IMPORTANT: Using 'docker compose down' WITHOUT '-v' flag to preserve volumes
            # This ensures MinIO data (and other persistent data) is NOT deleted
            docker compose down --remove-orphans

            # 2. Hapus volume anonim (sampah hex) yang tidak terpakai
            docker volume ls -q | grep -E '^[a-f0-9]{64}$' | xargs -r docker volume rm || true

            # 3. Hapus semua image yang tidak terpakai untuk memastikan build fresh
            docker image prune -af

            # 4. Hapus build cache Docker untuk memastikan build benar-benar fresh
            docker builder prune -af

            # 5. Ambil source code terbaru dari branch main
            git pull origin main

            # 6. Buat file .env dari secrets GitHub
            echo "${{ secrets.ENTIRE_ENV_FILE }}" > .env

            # 7. Build ulang dengan --no-cache dan --pull untuk memastikan base image juga fresh
            docker compose build --no-cache --pull

            # 8. Jalankan migrasi di container sementara
            docker compose run --rm ganeshadcert-api npx prisma migrate deploy

            # 9. Jalankan semua service di background
            docker compose up -d

            # 10. Cleanup image yang tidak terpakai lagi
            docker image prune -af
            docker builder prune -af
